//
//  PriorityQueue.swift
//  DataStructureTests
//
//  Created by 윤주원 on 2023/01/18.
//

import Foundation

/*
 Priority Queue, a queue where the most "important" items are at the front of the queue.
 
 The heap is a natural data structure for a priority queue, so this object(priority queue) simply wraps the Heap struct.
 
 All opearation are 0(log n)
 
 */

public struct PriorityQueue<T>{
//    private var heap : Heap<T>
}
